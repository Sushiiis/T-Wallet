apiVersion: v1
kind: Service
metadata:
  name: t-wallet
spec:
  selector:
    app: t-wallet
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP