# observability/prometheus.yml
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: t-wallet
    static_configs:
      - targets: ["host.docker.internal:8080"]   # сервис слушает вне docker-сети